<!DOCTYPE html>
<html lang="en">
<head>
  
  <!-- Meta Tags -->
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Apply to <%= agency.name %> | Modol</title>
  
  <!-- Favicon -->
  <link rel="icon" href="/images/fevicon.png" type="image/gif" />
  
  <!-- CSS Files -->
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/application.css">
  <link rel="stylesheet" href="/css/responsive.css">
  <link rel="stylesheet" href="/css/jquery.mCustomScrollbar.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <!-- Header Section -->
  <header class="header_section">
    <div class="container-fluid">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">
          <img src="/images/logo.png" alt="Modol Logo">
        </a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="/model">
                <i class="fas fa-arrow-left"></i> Back to Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">
                <i class="fas fa-sign-out-alt"></i> Logout
              </a>
            </li>
          </ul>
        </div>
      </nav>
    </div>
  </header>

  <div class="application-container">
    <div class="application-header">
      <% if (agency.image) { %>
        <img src="<%= agency.image %>" alt="<%= agency.name %> logo" class="agency-logo">
      <% } else { %>
        <div class="agency-logo bg-light d-flex align-items-center justify-content-center">
          <i class="fas fa-building text-muted" style="font-size: 2rem;"></i>
        </div>
      <% } %>
      <h2>Apply to <%= agency.name %></h2>
      <p class="text-muted">Please fill out the application form below</p>
    </div>

    <form action="/model/apply" method="POST" enctype="multipart/form-data">
      <input type="hidden" name="agencyId" value="<%= agency._id %>">
      
      <div class="form-group">
        <label for="message">Why do you want to join this agency?</label>
        <textarea name="message" class="form-control" id="message" rows="5" 
          placeholder="Tell us about your experience and why you'd be a good fit..." required></textarea>
      </div>
      
      <div class="form-group">
        <label for="portfolio">Upload Your Portfolio</label>
        <div class="custom-file">
          <input type="file" name="portfolio" class="form-control" id="portfolio" 
            accept=".pdf,.jpg,.jpeg,.png" required>
          <small class="form-text text-muted">PDF or image files (max 5MB)</small>
        </div>
      </div>
      
      <button type="submit" class="btn btn-apply mt-4">
        <i class="fas fa-paper-plane mr-2"></i> Submit Application
      </button>
    </form>
  </div>

  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>